# Module that contains PI vlaue
import math
# Take Radius from User
radius = int(input("Please Enter the Radius of the Circle: "))
# Print the area of the circle
print(f"Area of the Circle = {radius * radius * math.pi}")
